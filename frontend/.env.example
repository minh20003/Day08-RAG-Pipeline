# Để trống để sử dụng mock RAG tích hợp sẵn.
# Khi backend sẵn sàng, đặt URL API, ví dụ: http://localhost:8000
# Use `same-origin` when FastAPI serves frontend/dist and the API from one host.
VITE_RAG_API_URL=
